<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.casd.dao.hr.HtrainingrecordDao">
    <!-- 解决表名与字段不匹配 -->
    <!-- type 类的全限定名, 或者一个类型别名
         id 当前命名空间中的一个唯一标识，用于标识一个result map
         result 注入到字段或 JavaBean 属性的普通结果
     -->

    
    <!-- id 在命名空间中唯一的标识符，可以被用来引用这条语句
         resultMap 外部 resultMap 的命名引用
         resultType 从这条语句中返回的期望类型的类的完全限定名或别名。
     -->
     <insert id="insertRecord" parameterType="com.casd.entity.hr.Htrainingrecord">        
            INSERT into hr_training_record(id,user_id,begin_date,end_date,train_theme,train_mechanism,certifications)
            VALUES (#{id},#{user_id},#{begin_date},#{end_date},#{train_theme},#{train_mechanism},#{certifications})
            
            on duplicate key update id=#{id},user_id=#{user_id},begin_date=#{begin_date},end_date=#{end_date},
            train_theme=#{train_theme},train_mechanism=#{train_mechanism},certifications=#{certifications}
     </insert>
      
    <select id="getRainingrecord" parameterType="map" resultType="map">
         select ${fields} from ${where} 
      </select>
    
      <delete id="deleteHrecord" parameterType="map">
        DELETE FROM hr_training_record ${where}
    </delete>
    
</mapper>